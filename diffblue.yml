# Copyright 2019 Diffblue Limited. All Rights Reserved.
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
  # Constrain the size of arrays created in the analyzed code.
  java-max-vla-length: 100000
phases:
  -
    # Phase 0 PRETTY, SIMPLE and SMART - pretty tests, do not test private methods directly, Smart-Harness
    timeout: 180
    cbmcArguments:
      # Use simple models first containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Instantiate classes using the simplest constructor strategy
      smart-harness: simplest-constructor-and-nondet
  -
    # Phase 1 PRETTY AND COMPLEX - standard models only, pretty tests, do not test private methods directly, input factory
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
      # Limit input factory recursive depth (default 0 = no recursion allowed)
      java-test-input-factory-bmc-recursion-limit: 4
  -
    # Phase 2 FIFO Strategy - pretty tests, do not test private methods directly, fifo
    timeout: 100
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Add constraint that strings are printable
      max-nondet-string-length: 100
      # Explores the program tree breadth-first.
      paths: fifo
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: false
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
    # Phase 3 Exceptions - do not test private methods directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
    # Phase 4 Direct testing - test private methods directly, increased reflection
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value
      string-input-value: ["foo", "Bar", "BAZ", "1234", "1a 2b 3c", "A1B2C3", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c", ",", "/", "'"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
    # PHASE 5 Mocking - aggressive mocking, do not test private methods directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers first (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Only load the class (and related inner/outer classes) containing the method under test,
      load-containing-class-only: true
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [private]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
units:
 - target:
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/utils/ObjectUtils.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/utils/ObjectUtils.java
 - target:
   - dir: qmq-store/target/classes
     file: qunar/tc/qmq/store/MagicCodeSupport.class
     sourceDir: qmq-store/src/main/java
     sourceFile: qunar/tc/qmq/store/MagicCodeSupport.java
 - target:
   - dir: qmq-delay-server/target/classes
     file: qunar/tc/qmq/delay/base/LongHashSet.class
     sourceDir: qmq-delay-server/src/main/java
     sourceFile: qunar/tc/qmq/delay/base/LongHashSet.java
 - target:
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/metrics/Metrics.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/metrics/Metrics.java
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/metrics/QmqMeter.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/metrics/QmqMeter.java
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/metrics/QmqCounter.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/metrics/QmqCounter.java
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/metrics/MockRegistry.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/metrics/MockRegistry.java
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/metrics/QmqMetricRegistry.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/metrics/QmqMetricRegistry.java
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/metrics/QmqTimer.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/metrics/QmqTimer.java
 - target:
   - dir: qmq-server/target/classes
     file: qunar/tc/qmq/utils/ConsumerGroupUtils.class
     sourceDir: qmq-server/src/main/java
     sourceFile: qunar/tc/qmq/utils/ConsumerGroupUtils.java
 - target:
   - dir: qmq-store/target/classes
     file: qunar/tc/qmq/store/GroupAndSubject.class
     sourceDir: qmq-store/src/main/java
     sourceFile: qunar/tc/qmq/store/GroupAndSubject.java
 - target:
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/utils/Flags.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/utils/Flags.java
 - target:
   - dir: qmq-client/target/classes
     file: qunar/tc/qmq/tracing/TraceUtil.class
     sourceDir: qmq-client/src/main/java
     sourceFile: qunar/tc/qmq/tracing/TraceUtil.java
   - dir: qmq-client/target/classes
     file: qunar/tc/qmq/tracing/QmqMessageInjectAdapter.class
     sourceDir: qmq-client/src/main/java
     sourceFile: qunar/tc/qmq/tracing/QmqMessageInjectAdapter.java
   - dir: qmq-api/target/classes
     file: qunar/tc/qmq/Message.class
     sourceDir: qmq-api/src/main/java
     sourceFile: qunar/tc/qmq/Message.java
   - dir: qmq-client/target/classes
     file: qunar/tc/qmq/tracing/QmqMessageExtractAdapter.class
     sourceDir: qmq-client/src/main/java
     sourceFile: qunar/tc/qmq/tracing/QmqMessageExtractAdapter.java
 - target:
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/utils/CharsetUtils.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/utils/CharsetUtils.java
 - target:
   - dir: qmq-common/target/classes
     file: qunar/tc/qmq/utils/Bytes.class
     sourceDir: qmq-common/src/main/java
     sourceFile: qunar/tc/qmq/utils/Bytes.java
